it('should be {{description}}', function(done){
  api.get('{{path}}')
  .set('Accept', 'application/json')
  .expect({{responseCode}})
  .end(function(err, res){
    {{assertion}};
    done();
  })	
});
