      it('should be {{description}}', function({{#if asynchronous}}done{{/if}}) {
        request({
          url: '{{url}}',
          method: 'PUT',
          headers: {'Content-Type': 'application/json'},
          json: {
            {{#each parameters}}
            {{this.name}}: 'DATA GOES HERE',
            {{/each}}
          }
        },
        function(error, res, body) {
          {{#compare assertion 'expect'}}
          chai.expect(body).to.have.property('name');
          {{/compare}}
          {{#compare assertion 'should'}}
          body.should.have.property('name');
          {{/compare}}
          {{#compare assertion 'assert'}}
          chai.assert.property(body, 'name');
          {{/compare}}
          {{#if asynchronous}}done();{{/if}}
        });
      });
